/**
 * Lista de tipos de expresiones que se pueden encontrar en la sintaxis estructura
 */
public enum ExpresionType {
    NIL,
    SYMBOL,
    IDENTIFIER_OR_NUMERAL_OR_STRING,
    ESTRUCTURA,
    END
}
